type TRole = {
  SUPER_ADMIN: string;
  MANAGER: string;
  SELLER: string;
};

export const USER_ROLE: TRole = {
  SUPER_ADMIN: 'super-admin',
  MANAGER: "manager",
  SELLER: 'seller',
};

export const statusEnum=["active","blocked"]

export const roleEnum :string[]=["super-admin", "manager", "seller"]

